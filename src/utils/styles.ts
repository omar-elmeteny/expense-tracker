const styles = {
        gridContainer: {
            display: "grid",
            gridTemplateColumns: "repeat(4, 1fr)", // 3-4 per row
            gap: "16px",
            justifyContent: "center", // Keeps cards centered
            padding: "10px",
            maxWidth: "800px", // Controls max width for centering
            margin: "0 auto", // Centers the grid container
        }
    }

export default styles;